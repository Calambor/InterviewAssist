CREATE TABLE user (
    id TEXT NOT NULL,
    name TEXT NOT NULL
);

INSERT INTO user (id, name)
VALUES ("sample_user_1", "Mr. Sample");

selectAll:
SELECT *
FROM user;

createUser:
INSERT INTO user (id, name)
VALUES (:id, :name);
